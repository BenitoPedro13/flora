/**
 * Hex values for `next/og` ImageResponse — mirrors AlignUI tokens in
 * `app/globals.css` (ImageResponse cannot resolve CSS custom properties).
 */
export const OG_COLORS = {
  primary: "#22a06b",
  primaryDark: "#178459",
  background: "#ffffff",
  title: "#0e121b",
  subtitle: "#525866",
  muted: "#868c98",
  accent: "#ecfdf3",
} as const;
